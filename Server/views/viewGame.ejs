<nav class="navbar navbar-default">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Oh!</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href="/list-game">List Game <span class="sr-only">(current)</span></a></li>
        <li class="active"><a href="/create-game">Create Game</a></li>
      </ul>

      <form class="navbar-form navbar-right" role="search">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
      </form>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
<form action="/v1/player/update-game" style="width: 400px; margin: 0 auto;" method="POST">
  <h3><%= (game) ? game.title : ''%></h3>
  <input type="hidden" name="id" value="<%= (game && game.id) ? game.id : ''%>">
  <div>
    <h5>Tiêu đề</h5>
    <input type="text" name="title" value="<%= (game && game.title) ? game.title : ''%>" placeholder="Title">
  </div>
  <div>
    <h5>Slug</h5>
    <input type="text" name="slug" value="<%= (game && game.slug) ? game.slug : ''%>" placeholder="Slug">
  </div>
  <div>
    <h5>Background</h5>
    <img src="<%= (game && game.background) ? game.background : '' %>" width="200px">
    <input type="text" name="background" value="<%= (game && game.background) ? game.background : ''%>" placeholder="http://">
  </div>
  <div>
    <h5>Thumbnail</h5>
    <img src="<%= (game && game.thumb) ? game.thumb : '' %>" width="200px">
    <input type="text" name="thumb" value="<%= (game && game.thumb) ? game.thumb : ''%>" placeholder="http://">
  </div>
  <div>
    <h5>Player</h5>
    <input type="text" name="numberPlayers" value="<%= (game && game.numberPlayers) ? game.numberPlayers : '1'%>" placeholder="">
  </div>
  <div>
    <h5>Plays</h5>
    <input type="text" name="countPlays" value="<%= (game && game.countPlays) ? game.countPlays : '1'%>" placeholder="">
  </div>
  <div>
    <h5>Status</h5>
    <select name="status" id="status">
      <option value="0" <%= (game && game.status === 0) ? 'selected' : ''%>>Disable</option>
      <option value="1" <%= (game && game.status === 1) ? 'selected' : ''%>>Enable</option>
    </select>
  </div>
  <br/>============================================== <br/>
  <h4>Ảnh trong game</h4>

  <div>
    <h5>Ảnh cấp 1</h5>
    <img src="<%= (game) ? game.imagesInGame[0] : '' %>" width="107px">
    <input type="text" name="imagesInGame[0]" value="<%= (game && game.imagesInGame[0]) ? game.imagesInGame[0] : ''%>" placeholder="">
  </div>
  <div>
    <h5>Ảnh cấp 2</h5>
    <img src="<%= (game) ? game.imagesInGame[1] : '' %>" width="107px">
    <input type="text" name="imagesInGame[1]" value="<%= (game && game.imagesInGame[1]) ? game.imagesInGame[1] : ''%>" placeholder="">
  </div>
  <div>
    <h5>Ảnh cấp 3</h5>
    <img src="<%= (game) ? game.imagesInGame[2] : '' %>" width="107px">
    <input type="text" name="imagesInGame[2]" value="<%= (game && game.imagesInGame[2]) ? game.imagesInGame[2] : ''%>" placeholder="">
  </div>
  <div>
    <h5>Ảnh cấp 4</h5>
    <img src="<%= (game) ? game.imagesInGame[3] : '' %>" width="107px">
    <input type="text" name="imagesInGame[3]" value="<%= (game && game.imagesInGame[3]) ? game.imagesInGame[3] : ''%>" placeholder="">
  </div>
  <div>
    <h5>Ảnh cấp 5</h5>
    <img src="<%= (game) ? game.imagesInGame[4] : '' %>" width="107px">
    <input type="text" name="imagesInGame[4]" value="<%= (game && game.imagesInGame[4]) ? game.imagesInGame[4] : ''%>" placeholder="">
  </div>
  <div>
    <h5>Ảnh cấp 6</h5>
    <img src="<%= (game) ? game.imagesInGame[5] : '' %>" width="107px">
    <input type="text" name="imagesInGame[5]" value="<%= (game && game.imagesInGame[5]) ? game.imagesInGame[5] : ''%>" placeholder="">
  </div>
  <div>
    <h5>Ảnh cấp 7</h5>
    <img src="<%= (game) ? game.imagesInGame[6] : '' %>" width="107px">
    <input type="text" name="imagesInGame[6]" value="<%= (game && game.imagesInGame[6]) ? game.imagesInGame[6] : ''%>" placeholder="">
  </div>
  <div>
    <h5>Ảnh cấp 8</h5>
    <img src="<%= (game) ? game.imagesInGame[7] : '' %>" width="107px">
    <input type="text" name="imagesInGame[7]" value="<%= (game && game.imagesInGame[7]) ? game.imagesInGame[7] : ''%>" placeholder="">
  </div>
  <div>
    <h5>Ảnh cấp 9</h5>
    <img src="<%= (game) ? game.imagesInGame[8] : '' %>" width="107px">
    <input type="text" name="imagesInGame[8]" value="<%= (game && game.imagesInGame[8]) ? game.imagesInGame[8] : ''%>" placeholder="">
  </div>
  <div>
    <h5>Ảnh cấp 10</h5>
    <img src="<%= (game) ? game.imagesInGame[9] : '' %>" width="107px">
    <input type="text" name="imagesInGame[9]" value="<%= (game && game.imagesInGame[9]) ? game.imagesInGame[9] : ''%>" placeholder="">
  </div>
  <div>
    <h5>Ảnh cấp 11</h5>
    <img src="<%= (game) ? game.imagesInGame[10] : '' %>" width="107px">
    <input type="text" name="imagesInGame[10]" value="<%= (game && game.imagesInGame[10]) ? game.imagesInGame[10] : ''%>" placeholder="">
  </div>
  <div>
    <h5>Ảnh cấp 12</h5>
    <img src="<%= (game) ? game.imagesInGame[11] : '' %>" width="107px">
    <input type="text" name="imagesInGame[11]" value="<%= (game && game.imagesInGame[11]) ? game.imagesInGame[11] : ''%>" placeholder="">
  </div>
  <div>
    <h5>Ảnh cấp 13</h5>
    <img src="<%= (game) ? game.imagesInGame[12] : '' %>" width="107px">
    <input type="text" name="imagesInGame[12]" value="<%= (game && game.imagesInGame[12]) ? game.imagesInGame[12] : ''%>" placeholder="">
  </div>
  <div>
    <h5></h5>
    <input type="submit" value="Submit">
  </div>


</form>
